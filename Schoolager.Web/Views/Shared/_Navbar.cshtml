<nav class="navbar fixed-top navbar-expand-sm box-shadow bg-white" id="navbar">
    <div class="container-fluid p-0">
        <div class="navbar-collapse collapse d-flex justify-content-between">
            @if (User.Identity.IsAuthenticated)
            {
                <div class="burger-container ms-3">
                    <button id="burgerBtn" class="btn btn-outline-dark">X</button>
                </div>
            }
            <ul class="navbar-nav navbar-right">
                @if (User.Identity.IsAuthenticated)
                {  
                    <li class="">
                        <div class="dropdown me-4">
                            <button class="dropdown-toggle d-flex justify-content-center align-items-center" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
                            <div id="loggedUserImage" class="frame frame-nav me-2" style="background-image: url(@Url.Content("~/images/sample-user.jpg"))"></div>
                            <div class="d-flex flex-column ms-2">
                                <span class="text-dark fs-6 fw-light text-start" id="loggedUserName">Aluno Fixe</span>
                            </div>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark" aria-labelledby="dropdownMenuButton">
                                <li>
                                    <a class="dropdown-item text-light fw-lighter" asp-area="" asp-controller="Account" asp-action="RecoverPassword">Recover Password</a>
                                </li>
                                @if(User.IsInRole("Admin"))
                                {
                                    <li>
                                        <a class="dropdown-item text-light fw-lighter" asp-area="" asp-controller="Lessons" asp-action="SetSchoolYear">School Year</a>
                                    </li>
                                }
                                <li>
                                    <a class="dropdown-item text-light fw-lighter" asp-area="" asp-controller="Account" asp-action="Logout">
                                Logout
                                    </a>
                                </li>
                            </ul>
	                    </div>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>

